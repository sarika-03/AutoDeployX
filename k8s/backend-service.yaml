apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: autodeployx
  labels:
    app: backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
  - name: http
    protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30500